[{"setUp()": "void\n    {\n        parent::setUp();\n        $this->behaviorFlowService = app(BehaviorFlowService::class);\n        $this->tenantContextService = app(TenantContextService::class);\n        Cache::flush();"}, {"Cache": "flush();\n        parent::tearDown();"}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Test Event',\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Analyze behavior flow\n        $result = $this->behaviorFlowService->analyzeBehaviorFlow($definition->id);\n\n        // Verify structure\n        $this->assertIsArray($result);\n        $this->assertArrayHasKey('definition_id', $result);\n        $this->assertArrayHasKey('definition_name', $result);\n        $this->assertArrayHasKey('period', $result);\n        $this->assertArrayHasKey('flow_graph', $result);\n        $this->assertArrayHasKey('metrics', $result);\n        $this->assertArrayHasKey('common_paths', $result);\n        $this->assertArrayHasKey('optimization_suggestions', $result);\n\n        // Verify values\n        $this->assertEquals($definition->id, $result['definition_id']);\n        $this->assertEquals('Test Event', $result['definition_name": ""}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Page View": "Create users and events\n        $user1 = User::factory()->create();\n        $user2 = User::factory()->create();\n\n        // Create events with timestamps\n        CustomEvent::factory()->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user1->id,\n            'timestamp": "now()->subMinutes(10)", "CustomEvent": "factory()->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user2->id,\n            'timestamp' => now()->subMinutes(5),\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Analyze behavior flow\n        $result = $this->behaviorFlowService->analyzeBehaviorFlow($definition->id);\n\n        // Verify metrics\n        $this->assertEquals(2, $result['metrics']['total_users']);\n        $this->assertEquals(2, $result['metrics']['total_paths']);\n        $this->assertGreaterThan(0, $result['metrics']['unique_events": ""}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Test Event": "Create events at different times\n        $user = User::factory()->create();\n\n        CustomEvent::factory()->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user->id,\n            'timestamp": "now()->subDays(10)", "CustomEvent": "factory()->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user->id,\n            'timestamp' => now()->subDays(2),\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Analyze with date range (last 5 days)\n        $startDate = now()->subDays(5)->toDateString();\n        $endDate = now()->toDateString();\n        $result = $this->behaviorFlowService->analyzeBehaviorFlow($definition->id, $startDate, $endDate);\n\n        // Verify only recent events are included\n        $this->assertEquals(1, $result['metrics']['total_users": ""}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Checkout Flow',\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Analyze funnel\n        $result = $this->behaviorFlowService->analyzeFunnel($definition->id);\n\n        // Verify structure\n        $this->assertIsArray($result);\n        $this->assertArrayHasKey('definition_id', $result);\n        $this->assertArrayHasKey('definition_name', $result);\n        $this->assertArrayHasKey('period', $result);\n        $this->assertArrayHasKey('funnel_steps', $result);\n        $this->assertArrayHasKey('metrics', $result);\n        $this->assertArrayHasKey('drop_off_points', $result);\n        $this->assertArrayHasKey('optimization_suggestions', $result);\n\n        // Verify values\n        $this->assertEquals($definition->id, $result['definition_id']);\n        $this->assertEquals('Checkout Flow', $result['definition_name": ""}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Registration Flow": "Create users and events\n        $users = User::factory()->count(5)->create();\n\n        foreach ($users as $user) {\n            CustomEvent::factory()->create([\n                'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n                'user_id' => $user->id,\n                'timestamp": "now()->subMinutes(rand(1", "test_build_flow_graph_creates_correct_structure()": "void\n    {\n        // Create tenant and event definition\n        $tenant = Tenant::factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Test Event": "Create events\n        $user = User::factory()->create();\n        CustomEvent::factory()->count(3)->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user->id,\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Build flow graph\n        $graph = $this->behaviorFlowService->buildFlowGraph($definition->id);\n\n        // Verify structure\n        $this->assertIsArray($graph);\n        $this->assertArrayHasKey('nodes', $graph);\n        $this->assertArrayHasKey('edges', $graph);\n        $this->assertIsArray($graph['nodes']);\n        $this->assertIsArray($graph['edges": ""}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Test Event": "Create events\n        $user = User::factory()->create();\n        CustomEvent::factory()->count(5)->create([\n            'tenant_id' => $tenant->id", "tenant_id' => $tenant->id,\n                'definition_id' => $definition->id,\n                'user_id": "user->id", "test_identify_common_paths_returns_sorted_results()": "void\n    {\n        // Create tenant and event definition\n        $tenant = Tenant::factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "test_generate_optimization_suggestions_returns_valid_suggestions()": "void\n    {\n        // Create tenant and event definition\n        $tenant = Tenant::factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user->id,\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Generate suggestions\n        $suggestions = $this->behaviorFlowService->generateOptimizationSuggestions($definition->id);\n\n        // Verify structure\n        $this->assertIsArray($suggestions);\n        foreach ($suggestions as $suggestion) {\n            $this->assertArrayHasKey('type', $suggestion);\n            $this->assertArrayHasKey('priority', $suggestion);\n            $this->assertArrayHasKey('description', $suggestion);\n            $this->assertArrayHasKey('recommendation', $suggestion);\n            $this->assertArrayHasKey('expected_impact', $suggestion);\n\n            // Verify valid types\n            $this->assertContains($suggestion['type'], ['drop_off', 'path_length', 'engagement']);\n            $this->assertContains($suggestion['priority'], ['low', 'medium', 'high": ""}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Test Event": "Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // First call - should compute\n        $result1 = $this->behaviorFlowService->analyzeBehaviorFlow($definition->id);\n\n        // Second call - should use cache\n        $result2 = $this->behaviorFlowService->analyzeBehaviorFlow($definition->id);\n\n        // Verify results are identical\n        $this->assertEquals($result1", "test_behavior_flow_with_no_events_returns_empty_results()": "void\n    {\n        // Create tenant and event definition\n        $tenant = Tenant::factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Test Event',\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Analyze behavior flow with no events\n        $result = $this->behaviorFlowService->analyzeBehaviorFlow($definition->id);\n\n        // Verify empty results\n        $this->assertEquals(0, $result['metrics']['total_users']);\n        $this->assertEquals(0, $result['metrics']['total_paths']);\n        $this->assertEmpty($result['common_paths": ""}, {"Tenant": "factory()->create();\n        $definition = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant->id", "name' => 'Test Event": "Create events at different times\n        $user = User::factory()->create();\n\n        CustomEvent::factory()->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user->id,\n            'timestamp": "now()->subDays(10)", "CustomEvent": "factory()->create([\n            'tenant_id' => $tenant->id", "definition_id' => $definition->id,\n            'user_id' => $user->id,\n            'timestamp' => now()->subDays(2),\n        ]);\n\n        // Set tenant context\n        $this->tenantContextService->setTenant($tenant->id);\n\n        // Analyze with date range (last 5 days)\n        $startDate = now()->subDays(5)->toDateString();\n        $endDate = now()->toDateString();\n        $result = $this->behaviorFlowService->analyzeFunnel($definition->id, $startDate, $endDate);\n\n        // Verify only recent events are included\n        $this->assertEquals(1, $result['metrics']['total_users": ""}, {"Tenant": "factory()->create();\n        $tenant2 = Tenant::factory()->create();\n\n        // Create event definitions for each tenant\n        $definition1 = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant1->id", "name' => 'Tenant 1 Event": "definition2 = CustomEventDefinition::factory()->create([\n            'tenant_id' => $tenant2->id", "name' => 'Tenant 2 Event": "Create events for tenant 1\n        $user1 = User::factory()->create();\n        CustomEvent::factory()->count(3)->create([\n            'tenant_id' => $tenant1->id", "definition_id' => $definition1->id,\n            'user_id": "user1->id", "User": "factory()->create();\n        CustomEvent::factory()->count(5)->create([\n            'tenant_id' => $tenant2->id", "definition_id' => $definition2->id,\n            'user_id' => $user2->id,\n        ]);\n\n        // Set tenant context to tenant 1\n        $this->tenantContextService->setTenant($tenant1->id);\n\n        // Analyze behavior flow for tenant 1\n        $result1 = $this->behaviorFlowService->analyzeBehaviorFlow($definition1->id);\n\n        // Verify only tenant 1 events are included\n        $this->assertEquals(1, $result1['metrics']['total_users']);\n        $this->assertEquals(3, $result1['metrics']['total_paths": ""}]