D:\DevCenter\abuilds\alumate\tests\Feature\ComponentLibrary\AnimatedCounterTest.php
 [x] `Statistics API` → it returns health check status
 [ ] `Statistics API` → it returns platform metrics
 [x] `Statistics API` → it returns single statistic by id
 [x] `Statistics API` → it returns 404 for non-existent statistic
 [x] `Statistics API` → it returns batch statistics
 [x] `Statistics API` → it validates batch request parameters
 [x] `Statistics API` → it handles partial failures in batch requests
 [x] `Statistics API` → it caches statistics data
 [ ] `Statistics API` → it clears cache when requested
 [ ] `Statistics Service Integration` → it handles database connection errors gracefully
 [x] `Statistics Service Integration` → it returns fallback values for database statistics when query fails

